apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  # NodePort - udostępnia port na węźle klastra, używany do testów lokalnych
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      # stały port węzła
      nodePort: 30080
